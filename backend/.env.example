ALLOWED_ORIGINS=http://localhost:3000

# Optional: enables grounded AI summaries. Without these variables,
# the API uses a deterministic analytics response engine.
AZURE_OPENAI_ENDPOINT=
AZURE_OPENAI_API_KEY=
AZURE_OPENAI_DEPLOYMENT=
